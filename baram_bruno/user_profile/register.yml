info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER}}account/register/"
  body:
    type: json
    data: |-
      {
        "username":"ftm",
        "email":"ftm@gmail.com",
        "password":"12345678",
        "password2":"12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
