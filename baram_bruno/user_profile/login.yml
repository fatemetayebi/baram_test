info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{SERVER}}account/login/"
  body:
    type: json
    data: |-
      {
        "username":"ftmm",
        "password":"12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
