info:
  name: dashboard
  type: http
  seq: 4

http:
  method: GET
  url: "{{SERVER}}account/dashboard/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
