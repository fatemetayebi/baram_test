info:
  name: create
  type: http
  seq: 5

http:
  method: POST
  url: "{{SERVER}}services/create/"
  body:
    type: json
    data: |-
      {
        "name":"خدمت یک",
        "service_type":"سئو"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
