info:
  name: update
  type: http
  seq: 1

http:
  method: PUT
  url: "{{SERVER}}services/update/2/"
  body:
    type: json
    data: |-
      {
        "name":"خدمت دو",
        "service_type":"سئو"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
