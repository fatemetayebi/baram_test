info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{SERVER}}services/delete/2/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
