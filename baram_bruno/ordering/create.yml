info:
  name: create
  type: http
  seq: 5

http:
  method: POST
  url: "{{SERVER}}orders/"
  body:
    type: json
    data: |-
      {
        "service": 1,
        "service_name": "خدمت یک",
        "service_type": "سایر",
        "order_status": "pending",
        "order_status_display": "Pending Approval",
        "description": ""
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
