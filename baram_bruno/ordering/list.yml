info:
  name: list
  type: http
  seq: 5

http:
  method: GET
  url: "{{SERVER}}orders/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
