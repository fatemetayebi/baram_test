info:
  name: delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{SERVER}}orders/2/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
